<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="fontFamily">@font/lobster_regular</item>
        <item name="app_title">@drawable/letters</item>
    </style>

    <!-- Base application dark theme. -->
    <style name="AppThemeDark" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="fontFamily">@font/lobster_regular</item>
        <item name="app_title">@drawable/letters_dark</item>
    </style>

    <style name="CornerButton">
        <item name="android:padding">16dp</item>
        <item name="android:scaleType">fitCenter</item>
        <item name="android:background">?android:attr/selectableItemBackgroundBorderless</item>
    </style>

    <style name="PrimaryButton">
        <item name="android:elevation">3dp</item>
        <item name="android:background">@drawable/primary_rounded</item>
    </style>

    <style name="ConfigureLabelText">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">?colorAccent</item>
        <item name="android:fontFamily">@font/lobster</item>
    </style>

    <style name="ConfigureItemText">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">?colorAccent</item>
        <item name="android:fontFamily">@font/lobster</item>
        <item name="android:background">@drawable/primary_rounded</item>
    </style>

</resources>
